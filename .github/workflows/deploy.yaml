name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build builder image with Docker Compose
        run: docker compose build hugo

      - name: Run Hugo build with proper baseURL
        run: |
          docker compose run --rm --name hugo_builder hugo \
            hugo --baseURL "https://garrett-kirsch.ceo" \
                  --destination /home/personal_website/public

      - name: Copy public/ from builder container
        run: |
          docker cp hugo_builder:/home/personal_website/public ./public
          docker rm -f hugo_builder

      - name: Add .nojekyll
        run: touch ./public/.nojekyll

      - name: Add CNAME file
        run: echo "garrett-kirsch.ceo" > ./public/CNAME


      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages

# name: Deploy Hugo site to GitHub Pages

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
#         with:
#           submodules: recursive  # Crucial if your Hugo theme is a submodule
#           fetch-depth: 0         # Fetch all history for .GitInfo and lastmod

#       - name: Build and Run Hugo
#         run: |
#           # Use -v to map the current runner directory to the container directory
#           # Use --rm to automatically delete the container when finished
#           docker compose run --rm \
#             -v ${{ github.workspace }}:/home/personal_website \
#             hugo \
#             hugo --baseURL "https://garrett-kirsch.ceo" \
#                  --destination /home/personal_website/public

#       - name: Add .nojekyll and CNAME
#         run: |
#           touch ./public/.nojekyll
#           echo "garrett-kirsch.ceo" > ./public/CNAME

#       - name: Deploy to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./public
#           publish_branch: gh-pages